---

apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: {{ meta.name }}
    game: minecraft
  name: {{ meta.name }}-minecraft
spec:
  host: {{ host | default(None) }}
  port:
    targetPort: web
  to:
    kind: Service
    name: {{ meta.name }}-minecraft
